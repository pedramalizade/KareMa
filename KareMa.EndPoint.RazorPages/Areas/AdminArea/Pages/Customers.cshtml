@page
@model KareMa.EndPoint.RazorPages.Areas.AdminArea.Pages.CustomersModel
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "مشتری ها | کاره ما";
    ViewData["Service"] = " active open";
    ViewData["PageTitle"] = "مشتری";
}
<div class="card p-5">
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success" role="alert">@TempData["SuccessMessage"]</div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger" role="alert">@TempData["ErrorMessage"]</div>
    }
    <div class="table-responsive text-nowrap">
        <a asp-page="AddCustomer" class="btn btn-primary">افزودن</a>
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>آیدی</th>
                    <th>عکس</th>
                    <th>نام </th>
                    <th>نام خانوادگی </th>
                    <th>عملیات</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                @foreach (var customer in Model.GetCustomers)
                {
                    <tr>
                        <td>@customer.Id</td>
                        <td>
                            @if (string.IsNullOrEmpty(customer.Image))
                            {
                                <img src="~/assets/img/none.png" width="100px" height="50px" />
                            }
                            else
                            {
                                <img src="@customer.Image" alt="@customer.Image" width="100px" height="50px" />
                            }
                        </td>
                        <td>@customer.FirstName</td>
                        <td>@customer.LastName</td>
                        <td>
                            <div class="dropdown">
                                <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                    <i class="bx bx-dots-vertical-rounded"></i>
                                </button>
                                <div class="dropdown-menu">
                                    <a asp-page="UpdateCustomer" asp-route-id="@customer.Id">
                                        <i class="bx bx-edit-alt me-1"></i> ویرایش
                                    </a>
                                    <form method="post" asp-page-handler="Delete" asp-route-id="@customer.Id" style="display:inline;">
                                        <button type="submit" class="dropdown-item" onclick="return confirm('مطمئنی؟')">
                                            <i class="bx bx-trash me-1"></i> حذف
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>